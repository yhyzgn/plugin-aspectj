apply plugin: 'maven'
apply from: 'config.gradle'

def config = project.ext

// 发布到本地maven仓库 命令：gradlew uploadArchives
uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri(project.projectDir.absolutePath + "/maven/")) {}
            pom.project {
                version config.pkg.version
                artifactId config.pkg.artifactId
                groupId config.pkg.groupId
                packaging 'jar'
                description config.pkg.desc
            }
        }
    }
}